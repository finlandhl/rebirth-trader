# API key files — never commit
users.json

# Environment files
.env

# Source code — NOT for public GitHub (storefront only)
/src/
/main.py
/streamer.py
/pyproject.toml
/uv.lock
/.python-version
/rebirth-trader.sh
/.claude/

# Generated cache
/tmp/gen_stats_cache.json
/tmp/gen_stats_offset.json

# Runtime state — never commit (append-only, grows 10MB+/h)
/logs/*

# Nested git repos (standalone marketing repo — managed separately)
/gitdocs/

# Python
__pycache__/
*.pyc
.venv/
